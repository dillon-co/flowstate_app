<%= form_for @blog, :url => blog_path, :html => { :multipart => true } do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :body, :class => 'ckeditor' %>
  <%= f.file_field :media %>
  <%= f.submit %>
<% end %>
